class Sync {
  constructor({

  }) {

  }

  getCurrentVersion() {

  }

  push() {

  }

  /**
   * - 拉取版本号
   *  - 如果客户端的版本号和服务器中的版本号一致的话，就不拉取数据
   *  - 否则就拉取数据
   * -
   */
  pull() {


  }
}

module.exports = Sync;
